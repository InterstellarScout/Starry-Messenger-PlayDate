Store downloaded source GIF/video files here before or during conversion.
`tools/gif_adapter.py` now writes original media into this folder, while the Playdate-ready converted frame sets remain under `Source/gifs/`.
